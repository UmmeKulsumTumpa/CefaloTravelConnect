## Functions to Test in `transport.validation`

1. **validateCreateTransport**
   - Returns [] for valid input
   - Fails for missing/invalid mode, invalid operator
2. **validateUpdateTransport**
   - Returns [] for valid input
   - Fails for invalid mode, invalid operator
