## Methods to Test in `PostService`

1. **createPost**
   - Calls repository.createPost and returns result
2. **updatePost**
   - Calls repository.updatePost and returns result
3. **getPostById**
   - Calls repository.getPostById and returns result (undefined if not found)
4. **getAllPosts**
   - Calls repository.getAllPosts with filters and returns result
5. **deletePost**
   - Calls repository.deletePost and returns result
6. **likePost**
   - Calls repository.likePost and returns result
