## Methods to Test in `TravelPlanTransportService`

1. **addPlanTransport**
   - Throws if plan not found
   - Calls repository.addPlanTransport and returns result
2. **getPlanTransport**
   - Throws if plan not found
   - Throws if transport not found
   - Calls repository.getPlanTransport and returns result
3. **updatePlanTransport**
   - Throws if plan not found
   - Throws if transport not found
   - Calls repository.updatePlanTransport and returns result
4. **deletePlanTransport**
   - Throws if plan not found
   - Throws if transport not found
   - Calls repository.deletePlanTransport and returns result
5. **getPlanTransports**
   - Throws if plan not found
   - Calls repository.getPlanTransports and returns result
